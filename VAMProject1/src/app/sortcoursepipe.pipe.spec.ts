import { SortcoursepipePipe } from './sortcoursepipe.pipe';

describe('SortcoursepipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortcoursepipePipe();
    expect(pipe).toBeTruthy();
  });
});
